# fields – list to show, in this order
# remove or comment out to hide
os
kernel
uptime
packages
shell
wm
terminal
cpu
gpu
memory
disk
colors

# disabled – kept for reference, uncomment to bring back
# host
# display
# displaymanager
# theme
# icons
# font
# cursor
# swap
# ip
# battery
# locale

# extra disks (add more mount points)
# disk=/home
# disk=/data

# appearance
# label_color: red, green, yellow, blue, magenta, cyan, white, or ANSI number
# 94 = bright blue, matches fastfetch's "keys" accent (#33b1ff)
label_color=94
# separator=─           (character for the title separator)
# shading_mode=ascii    (ascii, or opt into blocks / sextants)
# shading=.,-~:;=!*#$@  (characters for 3D shading, supports UTF-8)
# box=0                 (adds a box around the system-data, 0 = off, 1 = on)

# logo colors (override distro defaults) - monochrome blue to match label_color
# logo_inner: front/back face (94 = bright blue)
# logo_outer: extruded sides (34 = darker blue, for depth)
logo_inner=94
logo_outer=34

# 3d
# light=top-left        (top-left, top-right, top, left, right, front, bottom-left, bottom-right)
spin=y           (x, y, or xy)
# speed=1.0             (rotation speed)
# size=1.0              (logo scale, e.g. 2.0 for double size)
# depth=1.0             (3D extrusion depth, e.g. 3.0 for chunkier look)
# height=36             (override render height in rows)
